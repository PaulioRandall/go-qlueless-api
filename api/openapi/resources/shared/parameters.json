"wrap_with": {
  "name": "wrap_with",
  "in": "query",
  "description": "A dot `.` separated list of meta information properties that should be returned in place of the response.",
  "required": false,
  "schema": {
    "type": "string"
  },
  "example": "message.self.data"
}