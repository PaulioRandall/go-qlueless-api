"wrap": {
  "name": "wrap",
  "in": "query",
  "description": "If present wraps the response and includes meta information within the response.",
  "required": false,
  "schema": {
    "type": "string"
  }
}